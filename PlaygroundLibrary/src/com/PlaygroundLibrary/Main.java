package com.PlaygroundLibrary;

public class Main
{
    public static void main(String args[])
    {
        /*
        Created a jar file, possible commands to use:

        1.Playsite creation
        DoubleSwings ds = new DoubleSwings(2);
        Carousel c = new Carousel(20);
        Slide s = new Slide(8);
        BallPit b = new BallPit(10);

        2.Creation of a kid
        Kid kid = new Kid("Robert", (byte)12, 1558, false);

        3.Addition of kids to PlaySite
        ds.addKid(kid);
        s.addKid(kid);
        c.addKid(kid);
        b.addKid(kid);

        4.Removal a kid from:
        Playsite
        ds.removeKidFromPlaySite(which);
        Queue
        ds.removeKidFromQueue(which)

        5.Ask kid how much time they spent in playsites
        KidManager.askKid(which)

        6.Get Utilization of playsite
        ds.GetUtilizationPercentage();

        7.get total visitors
        KidManager.getTotalVisitors();
         */



    }
}
